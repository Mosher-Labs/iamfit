{
  "name": "@iamfit/cli",
  "version": "0.0.0",
  "private": true,
  "description": "Thin CLI wrapper around @iamfit/core, packaged for npm/homebrew",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@iamfit/core": "workspace:*"
  }
}
